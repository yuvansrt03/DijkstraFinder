  for (const [adjNode, edgeWeight] of myGraph[node]) {
  //     const newDistance = dis + edgeWeight;

  //     if (newDistance < dist[adjNode]) {
  //       dist[adjNode] = newDistance;
  //       pq.enqueue(newDistance, adjNode);
  //       //   parent[adjNode] = currentNode;
  //     }
  //   }